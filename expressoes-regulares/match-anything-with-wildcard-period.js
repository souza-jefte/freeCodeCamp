/**
 * Complete a regex unRegexpara que ele corresponda as cordas "run", "sun", "fun", "pun", "nun", e "bun". Sua regex deve usar o caractere curinga.
 */

let exampleStr = "Let's have fun with regular expressions!";
let unRegex = /.un/; // Change this line
let result = unRegex.test(exampleStr);
